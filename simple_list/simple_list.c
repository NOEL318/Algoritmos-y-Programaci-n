#include "simple_list.h"

SIMPLE_LIST * new()
{
SIMPLE_LIST * node= (SIMPLE_LIST *) malloc(sizeof(SIMPLE_LIST))
}
SIMPLE_LIST * insert(char c)
{

}

void display(SIMPLE_LIST * list, char * message){
print("\n%s\n", message);
SIMPLE_LIST * node= list;
int i;
for(i=0; node±=NULL; i++){
printf("%d: $c\n", i , node->info);
node= node->link;
}


}
